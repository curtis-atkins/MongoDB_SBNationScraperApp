function newArticles(scrapedData){
  scrapedData.each(function(i, element){
var scrape = [];
    scrape.push(articles)
        var image = article.image;
        var title = article.title;
        var link = article.link;
        var author = article.author;
        var timeStamp = article.timeStamp;
        })
    console.log(scrape);
  }

newArticles();
 console.log(scrape); 